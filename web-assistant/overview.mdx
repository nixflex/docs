---
title: "Web Assistant"
description: "Add a click-to-call and chat widget to any website"
icon: "comment-dots"
---

The Web Assistant is an embeddable widget that puts a floating **Call + Chat** launcher on any website. Visitors can call your AI phone number, or chat in text with the same agent. It is a single `<script>` tag - no build step, no dependencies.

<Note>
  Everything about the widget's look and behaviour is controlled by `data-*` attributes on the script tag. The design lives in the snippet, not in the number - so any number with an agent can power any design.
</Note>

## Quick start

<Steps>
  <Step title="Copy the snippet">
    Grab the embed code from your dashboard (Web Assistant tab), or use the example below.
  </Step>
  <Step title="Set your number">
    Put one of your numbers (with an agent attached) in `data-number`.
  </Step>
  <Step title="Paste it before the closing body tag">
    Drop the script just before `</body>` on any page. The launcher appears in the corner.
  </Step>
</Steps>

```html
<script
  src="https://nixflex.com/button.js"
  data-number="+441234567890"
  data-design="1"
  data-color="#25D366"
  data-name="Acme Coffee">
</script>
```

## How it works

- **Call** is a `tel:` link that dials `data-number` - it opens the phone dialer (shows "Call now" on mobile). No browser audio is used.
- **Chat** sends the visitor's message to the widget endpoint and shows the reply in the panel.

The widget is fully inbound and stateless - the browser sends the number and the conversation so far, and the endpoint returns the next reply.

## Attributes

| Attribute | Required | Default | Description |
|---|---|---|---|
| `data-number` | Yes | - | The phone number (with an agent attached) the widget calls and chats with. |
| `data-design` | No | `1` | Which preset design to use. Browse the designs in your dashboard and use the number shown there. |
| `data-color` | No | `#25D366` | Accent colour (hex) for the launcher and buttons. |
| `data-name` | No | agent name | The name shown on the widget. |
| `data-position` | No | `right` | Corner for the launcher: `right` or `left`. |
| `data-icon` | No | design default | Preset launcher icon. |
| `data-greeting` | No | design default | Greeting bubble - see below. |
| `data-chips` | No | design default | Quick-reply buttons for chat, separated by `\|` (e.g. `Book appointment\|Opening hours\|Prices`). |
| `data-api` | No | Nixflex endpoint | Advanced - override the chat endpoint URL. Leave as default. |

## Designs

Several preset looks are built in - browse them with live previews in your dashboard, where each shows its number. Set `data-design` to that number:

| # | Name | Good for |
|---|---|---|
| 1 | Modern AI | Tech and startups |
| 2 | Classic Support | Clinics and services |
| 3 | Salon & Beauty | Salons, spas, beauty |
| 4 | Real Estate | Property and agencies |
| 5 | Gym & Fitness | Gyms and studios |
| 6 | Hotel & Concierge | Hotels and hospitality |
| 7 | AI Assistant Pro | Premium flagship, voice-first |
| 8 | Platform / SaaS | Software and AI products |
| 9 | Voice Orb | Voice-first, animated |
| 10 | Aurora Bubble | Premium, futuristic |

## Greeting bubble

`data-greeting` controls the small speech bubble next to the launcher:

- `data-greeting="true"` (or `on`, `yes`, `1`) - show the default greeting ("Need help? Call or chat with us").
- `data-greeting="false"` (or `off`, `no`, `0`) - hide it.
- `data-greeting="Book your table today"` - show that exact custom text.
- If you omit it, some designs show a default greeting and others do not - the dashboard preview shows which.

The greeting is visual only - it does not start a chat or cost anything.

## The chat endpoint

The widget's chat calls:

```
POST https://api.nixflex.com/v1/widget/message
Content-Type: application/json

{ "number": "+441234567890", "history": [ ... ] }
```

It always responds with `200` and one of:

```json
{ "ok": true, "reply": "We are open Mon to Fri, 8:30am to 5:30pm." }
```

```json
{ "ok": false, "reason": "server_error" }
```

So the widget never shows a hard error to a visitor. This endpoint is public and needs no API key.

<Note>
  **CORS:** the chat endpoint allows requests from any origin, so the widget works on any customer website with no domain whitelisting.
</Note>

## Teaching the assistant what to say

The web assistant answers using the **same knowledge as your phone agent** - it already knows your business, services, hours, and prices from the agent's main prompt. You do not re-enter any of that.

What you *can* add is a short set of **web-only instructions**: how the assistant should behave in chat. In your dashboard, on the Web Assistant page, pick the agent and number, then click **Set up agent**. A box opens where you write rules like:

```
Keep replies under two sentences.
Always mention our free first check-up for new patients.
If someone asks about parking, tell them we have free spaces on Oak Lane.
```

These rules apply only to web chat - your phone calls are unaffected. Leave it blank and the assistant just uses the agent's main knowledge with sensible chat defaults.

<Note>
  There is no separate "knowledge base" to fill in and no file upload. The assistant already has your business knowledge from the agent. If you want it to know something extra, paste the text straight into the box - you can extract text from a PDF using any AI tool and paste it here.
</Note>

There is a limit of about **5,000 tokens** (~20,000 characters) on these instructions. The dashboard shows a live counter and the engine enforces the same cap, so a very long prompt is trimmed rather than slowing every reply.

## What the web assistant will and will not do

The web assistant is **answer-only by design**. It will:

- Answer questions about your business from the agent's knowledge
- Follow your web-only instructions
- Remember the current conversation (see below)

It will **not** book, reschedule, cancel, or change appointments over web chat - even if your main agent prompt says it can. This boundary is enforced in the engine, not left to the prompt, so it always holds. When a visitor wants to book or needs a person, the assistant tells them to call the business. Voice calls remain where bookings and other actions happen.

## Memory

Each visitor's browser keeps their own conversation and sends the recent history with every message, so the assistant remembers what was said earlier in the same chat (roughly the last 20 back-and-forth exchanges). Because each browser holds only its own history, two visitors chatting at the same time never see each other's messages - conversations are isolated by design. There is no shared server-side web memory, so nothing carries over between different visitors or between separate visits.

## Offering the widget to your users

If you run a platform and want to give the widget to your own end-users (each on their own website), you do not rebuild the designs - they already exist in `button.js`. You just choose which design each user gets and supply their number. Three ways:

<CardGroup cols={3}>
  <Card title="One design for all" icon="1">
    Hard-code a single `data-design` and only swap `data-number` per user. Simplest.
  </Card>
  <Card title="Let users pick" icon="2">
    Show a small design picker in your own app; write the chosen `data-design` into each user's snippet. Recommended.
  </Card>
  <Card title="Fully custom" icon="3">
    Build your own widget front-end and call `POST /v1/widget/message` directly with the user's number. Total control.
  </Card>
</CardGroup>

## Notes

- **Cost:** a chat message uses AI text usage; a call uses voice minutes (billed like any call). The greeting bubble is free.
- **One widget per page** is recommended.
- The widget script is served from `https://nixflex.com/button.js` and is safe to cache.